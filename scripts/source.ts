// Search-agent sourcing worker — implemented in M6.
// Will run Claude Code web-search agents to queue new candidates into the DB.
console.log("source worker: not implemented yet (M6).");
process.exit(0);
